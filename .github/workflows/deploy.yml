name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: yarn

      - name: Build project
        run: yarn build

      - name: Deploy to gh-pages
        run: |
          # Initialize a new Git repository in the build output folder
          cd dist  # Make sure this path matches your build output folder
          git init
          git add .
          git commit -m "Deploy to gh-pages"

          # Set GitHub Actions as the user
          git config user.name "Sully"
          git config user.email "147931276+sullytobias@users.noreply.github.com"

          # Push to gh-pages branch using the PAT
          git push --force "https://${{ secrets.GH_TOKEN }}@github.com/sullytobias/sullytobias.github.io.git" HEAD:gh-pages